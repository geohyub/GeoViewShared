"""GeoView CustomTkinter Widget Library v3.

Provides reusable, themed widgets for all GeoView desktop applications.

Widgets:
    GeoViewApp       — Base application class v3 (sidebar, header, statusbar)
    ParamInput       — Labeled input field with units
    ResultCard       — Colored result display with status badge
    SectionHeader    — Navy section header
    ChartFrame       — Matplotlib chart embedded in CTk
    PipelineTracker  — Multi-stage QC pipeline visualization
    ScoreGauge       — Animated score card with grade badge
    KPICard          — Professional metric display card
    DataTable        — Pure CTk scrollable data table
    ActivityLog      — Terminal-style colored log output
"""
